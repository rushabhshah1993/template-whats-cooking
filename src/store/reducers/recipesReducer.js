/*
    This is a blank file for a simple reducer to store recipes.
    Feel free to modify this file or use this as a template to 
    write your own reducers.
*/

const initialState = {};

const reducer = (state=initialState, action) => {
    switch(action.type) {
        default: return state;
    }
}

export default reducer;
